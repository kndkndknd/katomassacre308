<!DOCTYPE html>
<html lang="ja">
<head>
  <title><%= title %></title>
  <LINK rel="stylesheet" type="text/css" href="/css/client.css">
</head>
<body id="body">
  <script src="/socket.io/socket.io.js"></script>
  <div id="wrapper">
  <textarea name="text" id="text" rows="2" cols="60" style="font-size:30px;"></textarea> <br>
  <input type="button" id="rec" value="rec" />
  <input type="button" id="textActivate" value="text有効化" />
  <input type="button" id="playback" value="playback" />
  <input type="button" id="end" value="end" /><br>
  <video id="video" playsinline controls="true" style="display:none;"></video>
  <canvas id="cnvs">
    canvas not applied.
  </canvas>
  <div id="list"></div>
  </div>
  <script>
    var socket = io.connect();
    var video;
    var buffer = document.createElement('canvas');
    var client = "<%= title %>";
    //var mode = "none"; //"osc" or "feedback" or "whitenoise"
/*
    var cmdMode = {
      "sinewave": false,
      "whitenoise": false,
      "feedback": false,
      "instruction": false
    }
    */
    var videoMode = {"mode": "none", "option": "none"}; //"record" or "playback"
    var strings = "";
    var streamList;
  </script>
  <script src="/javascripts/ctrl.js"></script>
</body>
</html>
