<!DOCTYPE html>
<html lang="ja">
<head>
  <title><%= title %></title>
  <LINK rel="stylesheet" type="text/css" href="/css/client.css">
</head>
<body id="body">
  <script src="/socket.io/socket.io.js"></script>
  <div id="wrapper">
  <video id="video" playsinline controls="true" style="display:none;"></video>
  <canvas id="cnvs">
    canvas not applied.
  </canvas>
  <textarea name="text" id="inst" rows="7" cols="60" style="font-size:30px; display:none;"></textarea> <br>
  <script>
    var socket = io.connect();
    var video;
    var buffer = document.createElement('canvas');
    var client = "<%= title %>";
    var videoMode = {"mode": "none", "option": "none"}; //"record" or "playback"
    var strings = "";
    var streamList;
  </script>
  <script src="/javascripts/client.js"></script>
</body>
</html>
